
; rol ; rotate right
; ror ; rotate left
; rcl ; rotate left through the carry flag
; rcr ; rotate right through the carry flag
; [reg/mem],[imm/cl]


; ror bl, 3 ; This puts the right 3 cells in the left and move the rest of the cells to 3 at right to let space for the three moved bites

end